################################################################################
before running this script, 
you should run reactdiff program with "inputs_Schlogl_1d_analysis" and 
obtain "Schlogl.S_k.pair=1.Re.dat" in test directory.
################################################################################

generate a plot that compares numerical result of S(k) with theoretical results.

executing run.sh will
1) copy numerical result in test directory into the present directory;
2) generate a plot through gnuplot with Sk.plt.

** you will get an error that res_theo is not found. see below.

you will need to
1) change the following parameters in Sk.plt accordingly.
  - nb, r, D0 (these values are only problem specific)
  - dt, dx, rm, chi (these values give alpha and beta)
  - you may also want to change title, yrange, ytics, mytics.
2) provide "res_theo" containing theoretical result.
  - this can be calculated from Mathematica notebook "Schlogl_monomodal_Sk.nb"
